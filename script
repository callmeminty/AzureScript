-- UI generated by A1_exe's converter | Cleaned by ChatGPT for GitHub use

local Players = game:GetService("Players")
local player = Players.LocalPlayer

local function Create(class, parent, props)
	local inst = Instance.new(class)
	if parent then inst.Parent = parent end
	for prop, val in pairs(props or {}) do
		pcall(function() inst[prop] = val end)
	end
	return inst
end

local MainGui = Create("ScreenGui", player:WaitForChild("PlayerGui"), {
	Name = "MainGui",
	ResetOnSpawn = true,
	ZIndexBehavior = Enum.ZIndexBehavior.Sibling,
})

local Frame = Create("Frame", MainGui, {
	Name = "MainFrame",
	Size = UDim2.new(0, 484, 0, 289),
	Position = UDim2.new(0.357, 0, 0.288, 0),
	BackgroundColor3 = Color3.fromRGB(12, 12, 12),
	BackgroundTransparency = 0.05,
	BorderSizePixel = 0,
})

Create("UICorner", Frame)

Create("TextLabel", Frame, {
	Text = "Crackhead by",
	Size = UDim2.new(0, 148, 0, 50),
	Position = UDim2.new(0.0165, 0, 0.0207, 0),
	BackgroundTransparency = 1,
	TextColor3 = Color3.new(1, 1, 1),
	TextSize = 20,
	TextWrapped = true,
})

Create("TextLabel", Frame, {
	Text = "Lean",
	Size = UDim2.new(0, 148, 0, 50),
	Position = UDim2.new(0.196, 0, 0.0207, 0),
	BackgroundTransparency = 1,
	TextColor3 = Color3.fromRGB(119, 0, 255),
	TextSize = 20,
	TextWrapped = true,
})

Create("Frame", Frame, {
	Size = UDim2.new(0, 484, 0, 2),
	Position = UDim2.new(-0.002, 0, 0.193, 0),
	BackgroundColor3 = Color3.fromRGB(65, 65, 65),
	BorderSizePixel = 0,
})

local HomeButton = Create("Frame", Frame, {
	Name = "HomeButton",
	Size = UDim2.new(0, 109, 0, 44),
	Position = UDim2.new(0.0537, 0, 0.2802, 0),
	BackgroundColor3 = Color3.new(1, 1, 1),
	BorderSizePixel = 0,
})
Create("UICorner", HomeButton)
Create("UIGradient", HomeButton)

local Button = Create("Frame", Frame, {
	Name = "Button",
	Size = UDim2.new(0, 109, 0, 44),
	Position = UDim2.new(0.0537, 0, 0.474, 0),
	BackgroundColor3 = Color3.new(1, 1, 1),
	BorderSizePixel = 0,
	ZIndex = 554,
})
Create("UICorner", Button)
